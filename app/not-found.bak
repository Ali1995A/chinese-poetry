import Link from 'next/link';
import { Home, ArrowLeft, Search } from 'lucide-react';

export default function NotFound() {
  return (
    <div className="min-h-screen bg-[var(--background)] flex items-center justify-center px-4">
      <div className="max-w-md w-full text-center space-y-8">
        
        {/* 404 数字 */}
        <div className="relative">
          <div className="text-[120px] md:text-[160px] font-serif font-bold text-primary/10 leading-none">
            404
          </div>
          <div className="absolute inset-0 flex items-center justify-center">
            <div className="text-center">
              <div className="text-2xl font-serif font-bold text-primary mb-2">
                页面未找到
              </div>
              <div className="text-[var(--text-secondary)] font-serif">
                您寻找的诗篇可能已随风飘散
              </div>
            </div>
          </div>
        </div>

        {/* 描述文字 */}
        <div className="space-y-4">
          <p className="text-[var(--text-secondary)] font-serif leading-relaxed">
            此页面可能已被移动、删除，或者您输入了错误的地址。
            <br />
            让我们带您回到诗词的世界。
          </p>
        </div>

        {/* 操作按钮 */}
        <div className="flex flex-col sm:flex-row gap-4 justify-center">
          <Link 
            href="/"
            className="inline-flex items-center justify-center gap-2 bg-primary text-white px-6 py-3 rounded-lg font-serif font-medium hover:bg-primary-light transition-colors duration-300 shadow-card hover:shadow-card-hover"
          >
            <Home size={18} />
            返回首页
          </Link>
          
          <Link 
            href="/poems"
            className="inline-flex items-center justify-center gap-2 border border-[var(--border)] text-[var(--text-primary)] px-6 py-3 rounded-lg font-serif font-medium hover:border-primary hover:text-primary transition-colors duration-300"
          >
            <Search size={18} />
            浏览文库
          </Link>
        </div>

        {/* 装饰性诗句 */}
        <div className="pt-8 border-t border-[var(--border)]">
          <p className="text-sm text-[var(--text-secondary)] font-serif italic">
            "众里寻他千百度，蓦然回首，那人却在灯火阑珊处。"
          </p>
        </div>

      </div>
    </div>
  );
}